import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  emptyState: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    gap: 8,
  },
  emptyText: {
    color: '#ffffff',
    fontSize: 20,
    fontWeight: '700',
  },
  emptySubtext: {
    color: '#B0B4BA',
    fontSize: 14,
  },
});